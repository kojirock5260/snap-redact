# 変更履歴

[English](CHANGELOG.md)

## 1.0.0 — 2026-09-16

### 追加

- 要素をクリックして選べるようにした。範囲を決める前にページの要素をクリックすると、
  その要素の矩形がそのまま選択範囲になる。ポインタの下にある要素は点線の枠で示す。
  端を狙ってドラッグする代わりで、表のセル、アバター、1 行のメールアドレスに効く。
  ページの DOM は書き換えず、矩形を借りるだけ
- 要素をクリックして隠す / 囲えるようにした。範囲を決めたあと、「隠す」か「囲う」を
  持って範囲の中の要素をクリックすると、その要素にぴったりの図形が付く。矢印は向きが
  要るので対象外。座標は外側へ丸めるので、文字の縁が 1 ピクセル残ることはない
- 矢印キーで選択範囲を動かせるようにした。1px ずつ、Shift で 10px ずつ。撮った絵の
  外には出ない。図形は動かない。図形は絵の上の場所に付いているもので、切り出す範囲を
  ずらしても隠したい場所は変わらないため

### 変更

- 版を 1.0.0 にした。ストアで公開したので、0.x のままにしておく理由が無い
- 範囲を選ぶ前の案内を「ドラッグで範囲を、クリックで要素を選択」にした
- ヘルプに要素のクリックと矢印キーの行を足した
- 矢印キーは、範囲を決める前と何かを引いている最中はこれまで通り飲み込む。ページには渡さない
- 書き出す PNG を RGBA から RGB にした。出力に透けるピクセルは無いので、透明チャンネルは
  全部 255 のまま容量を食っていただけだった。同じ絵で 6% ほど小さくなる

## 0.5.0 — 2026-08-18

### 追加

- `Cmd+A` / `Ctrl+A` で撮った範囲全体を選べるようにした。効くのは範囲を決める前だけで、
  決めたあとは何も起きず、ページにも渡さない
- 範囲を選んでいる間、画面下に案内を出すようにした。ドラッグで選べることと全体選択の
  キーを載せてある。範囲が決まるとツールバーと入れ替わる

### 変更

- 端から 12px 以内を端に吸着させるようにした。オーバーレイはページのビューポートから
  出られず、OS のスクリーンショットのように窓の外から引き始められない。その代わりになる
- 窓に収まらないとき、ツールバーを複数行に折り返すようにした。行が変わるのは道具・色・
  出力・ヘルプのまとまりの間だけで、まとまりの途中では割れない
- まとまりの区切りを縦罫から間隔に変えた。折り返すと縦罫が行末に取り残され、何も
  区切らない線になるため
- ヘルプとトーストを、ツールバーの高さに関わらずその上に置くようにした。固定値での
  配置だと、2 段になったツールバーと重なる

### 修正

- DevTools のデバイスモードで、ドラッグの途中で選択範囲が止まる問題を直した。タッチの
  エミュレーションによりブラウザがドラッグをスクロールとして横取りし、ポインタイベントが
  打ち切られていた。`touch-action: none` で防ぎ、`pointercancel` も処理するようにした

0.5.0 より前のバージョンは、このファイルより古い。
